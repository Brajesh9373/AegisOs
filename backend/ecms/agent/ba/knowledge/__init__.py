"""Knowledge base for BA agent augmentation.

Stores categorized knowledge (patterns, anti-patterns, compliance rules,
domain expertise, corrections) with embeddings for semantic retrieval.
Powers the RAG-augmented BA agent so it draws from accumulated expertise
instead of starting from scratch each discovery.
"""
