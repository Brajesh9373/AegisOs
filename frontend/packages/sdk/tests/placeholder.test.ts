import { describe, it, expect } from 'vitest';
import { sdk_PLACEHOLDER } from '../src';

describe('sdk placeholder', () => {
  it('should be a placeholder', () => {
    expect(sdk_PLACEHOLDER).toBe(true);
  });
});
