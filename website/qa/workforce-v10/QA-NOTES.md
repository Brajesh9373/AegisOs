# Workforce V10 visual QA

- Dedicated /product/workforce experience replaces the generic product template.
- Desktop static layout QA: 1536px viewport.
- Mobile static layout QA: 390px viewport.
- Interactive states are implemented in the React component with Framer Motion; the QA HTML is a static structural mirror used to inspect spacing, hierarchy, and responsive layout.
- TypeScript/TSX syntax parse checks passed for the new component and routing changes.
